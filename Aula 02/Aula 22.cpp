#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
int main () {
	char nome
	 int numero 
	 bool t, f 
	
	//Solicitar o nome de usuário da pessoa, se ela não tiver, fará o cadastro
	printf("Você possui cadastro?\n tecle 1 se tiver e 0 se precisa fazer o cadastro")
	scanf("%d", t&&f);
	if ('resposta for 1')
	 printf("Digite sua letra de usuário");
	 scanf("%c", nome);
	 printf("Digite seu número de usuário");
	 scanf("%d");
	 
}
